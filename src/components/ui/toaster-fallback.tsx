// Simple Toaster component fallback
import React from 'react';

const ToasterFallback = () => {
  // This is a placeholder component
  // Once sonner is installed, you can replace this import in layout.tsx
  return null;
};

export { ToasterFallback as Toaster };
