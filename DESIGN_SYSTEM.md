# Design System Documentation\n\n## Overview\nThis document outlines the comprehensive design system implemented across the Complaint Management System. The design system ensures consistency, accessibility, and maintainability across all pages and components.\n\n## Design System Components\n\n### 📦 Core Components\nLocation: `/src/components/layout/pages/index.ts`\n\n#### PageContainer\nMain wrapper for all page content with consistent spacing.\n```tsx\n<PageContainer className=\"animate-fade-in\">\n  {/* Page content */}\n</PageContainer>\n```\n\n#### PageHeader\nStandardized page header with title, description, badges, and actions.\n```tsx\n<PageHeader\n  title=\"Page Title\"\n  description=\"Page description\"\n  badge={{\n    text: \"Status\",\n    variant: \"secondary\"\n  }}\n  actions={\n    <>\n      <Button variant=\"outline\">Action 1</Button>\n      <Button>Primary Action</Button>\n    </>\n  }\n/>\n```\n\n#### StatsGrid\nResponsive grid for displaying statistics with icons and colors.\n```tsx\nconst stats = [\n  {\n    title: 'Total Items',\n    value: 42,\n    icon: <FileText className=\"h-6 w-6\" />,\n    color: 'blue' as const,\n    description: 'All items in system',\n    trend: { value: 15, isPositive: true }\n  }\n];\n\n<StatsGrid stats={stats} columns={4} />\n```\n\n#### ContentCard\nWrapper for main content sections with optional header and footer.\n```tsx\n<ContentCard\n  title=\"Card Title\"\n  description=\"Card description\"\n  headerActions={<Button>Action</Button>}\n  footer={<div>Footer content</div>}\n>\n  {/* Card content */}\n</ContentCard>\n```\n\n#### FilterBar\nContainer for search and filter controls.\n```tsx\n<FilterBar>\n  <Input placeholder=\"Search...\" />\n  <Select>...</Select>\n</FilterBar>\n```\n\n#### EmptyState\nStandardized empty state with icon, message, and optional action.\n```tsx\n<EmptyState\n  icon={<FileText className=\"h-12 w-12\" />}\n  title=\"No items found\"\n  description=\"Try adjusting your filters\"\n  action={{\n    label: \"Reset Filters\",\n    onClick: () => resetFilters()\n  }}\n/>\n```\n\n#### ErrorState\nError display with retry functionality.\n```tsx\n<ErrorState\n  message=\"Failed to load data\"\n  onRetry={() => refetch()}\n/>\n```\n\n## 🎨 Design Tokens\n\n### Color System\nDefined in `/src/app/globals.css`\n\n#### Status Colors\n- **New**: Blue (`--new`)\n- **Assigned**: Yellow (`--assigned`)\n- **In Progress**: Purple (`--in-progress`)\n- **Completed**: Green (`--completed`)\n- **Closed**: Gray (`--closed`)\n\n#### Semantic Colors\n- **Success**: Green (`--success`)\n- **Warning**: Orange (`--warning`)\n- **Error**: Red (`--destructive`)\n- **Info**: Blue (`--info`)\n\n### Status Badge Classes\n```css\n.status-new { /* Blue styling */ }\n.status-assigned { /* Yellow styling */ }\n.status-in-progress { /* Purple styling */ }\n.status-completed { /* Green styling */ }\n.status-closed { /* Gray styling */ }\n```\n\n## 🏗️ Layout Structure\n\n### Dashboard Layout\nLocation: `/src/components/layout/DashboardLayout.tsx`\n\n- **Responsive sidebar** with role-based navigation\n- **Collapsible sidebar** for desktop\n- **Mobile-friendly** with overlay navigation\n- **Breadcrumb navigation** in header\n- **Theme toggle** and user menu\n\n### Page Structure\n```tsx\nfunction MyPage() {\n  return (\n    <PageContainer>\n      {/* 1. Page Header */}\n      <PageHeader />\n      \n      {/* 2. Statistics (optional) */}\n      <StatsGrid />\n      \n      {/* 3. Error/Loading States */}\n      {error && <ErrorState />}\n      \n      {/* 4. Filters (optional) */}\n      <FilterBar />\n      \n      {/* 5. Main Content */}\n      <ContentCard>\n        {/* Table, List, or other content */}\n      </ContentCard>\n      \n      {/* 6. Additional Sections (optional) */}\n      <Card>...</Card>\n    </PageContainer>\n  );\n}\n```\n\n## 🎭 Animation System\n\n### CSS Classes\n```css\n.animate-fade-in     /* Fade in animation */\n.animate-slide-up    /* Slide up animation */\n.animate-pulse-slow  /* Slow pulse animation */\n.card-hover          /* Card hover effects */\n.card-interactive    /* Interactive card effects */\n.table-row-hover     /* Table row hover */\n```\n\n### Usage\n```tsx\n<PageContainer className=\"animate-fade-in\">\n<Card className=\"card-hover\">\n<TableRow className=\"table-row-hover\">\n```\n\n## 📱 Responsive Design\n\n### Breakpoints\n- **Mobile**: < 768px\n- **Tablet**: 768px - 1024px\n- **Desktop**: > 1024px\n\n### Grid System\n```tsx\n{/* Responsive stats grid */}\n<StatsGrid columns={4} /> {/* 1/2/2/4 on mobile/tablet/desktop */}\n\n{/* Custom responsive grids */}\n<div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4\">\n```\n\n## 🔧 Utility Classes\n\n### Text Utilities\n```css\n.line-clamp-1  /* Single line with ellipsis */\n.line-clamp-2  /* Two lines with ellipsis */\n.line-clamp-3  /* Three lines with ellipsis */\n.text-gradient /* Gradient text effect */\n```\n\n### Layout Utilities\n```css\n.container-padding  /* Consistent container padding */\n.page-container     /* Max-width container with padding */\n.form-section       /* Form section styling */\n```\n\n### Interactive Utilities\n```css\n.btn-gradient       /* Gradient button styling */\n.glass              /* Glass morphism effect */\n.gradient-bg        /* Gradient background */\n```\n\n## 🎯 Loading States\n\n### Page Loading\n```tsx\nif (loading) {\n  return <PageSkeleton />;\n}\n```\n\n### Component Loading\n```tsx\n<StatsGridSkeleton columns={4} />\n<TableSkeleton rows={5} />\n```\n\n### Custom Skeletons\n```tsx\n<Skeleton className=\"h-8 w-64\" />\n<Skeleton className=\"h-4 w-48\" />\n```\n\n## 🌗 Theme Support\n\n### Dark Mode\nAutomatic theme switching with `ThemeProvider`\n- System preference detection\n- Manual theme toggle\n- Smooth transitions\n\n### Theme Variables\nAll colors use CSS custom properties for seamless theme switching:\n```css\ncolor: hsl(var(--foreground))\nbackground-color: hsl(var(--background))\n```\n\n## ♿ Accessibility\n\n### Focus Management\n- Visible focus indicators\n- Keyboard navigation support\n- Screen reader optimizations\n\n### Color Contrast\n- WCAG AA compliant colors\n- High contrast mode support\n- Color-blind friendly palette\n\n### Semantic HTML\n- Proper heading hierarchy\n- ARIA labels and descriptions\n- Semantic form structure\n\n## 🚀 Performance\n\n### Optimizations\n- Lazy loading of components\n- Efficient re-renders with React.memo\n- Optimized CSS with utility-first approach\n- Minimal JavaScript bundle size\n\n### Best Practices\n- Use design system components\n- Avoid inline styles\n- Leverage CSS custom properties\n- Implement proper loading states\n\n## 📝 Implementation Examples\n\n### Standard Page Implementation\n```tsx\n'use client';\n\nimport { useState, useEffect } from 'react';\nimport {\n  PageContainer,\n  PageHeader,\n  StatsGrid,\n  ContentCard,\n  FilterBar,\n  EmptyState,\n  ErrorState,\n  PageSkeleton\n} from '@/components/layout/pages';\n\nexport default function MyPage() {\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState('');\n  const [data, setData] = useState([]);\n\n  if (loading) return <PageSkeleton />;\n\n  return (\n    <PageContainer className=\"animate-fade-in\">\n      <PageHeader\n        title=\"My Page\"\n        description=\"Page description\"\n        actions={<Button>Add New</Button>}\n      />\n      \n      <StatsGrid stats={statsData} />\n      \n      {error && <ErrorState message={error} onRetry={refetch} />}\n      \n      <FilterBar>\n        <Input placeholder=\"Search...\" />\n        <Select>...</Select>\n      </FilterBar>\n      \n      <ContentCard title=\"Data Table\">\n        {data.length === 0 ? (\n          <EmptyState\n            icon={<FileText />}\n            title=\"No data\"\n            description=\"No items found\"\n          />\n        ) : (\n          <Table>\n            {/* Table content */}\n          </Table>\n        )}\n      </ContentCard>\n    </PageContainer>\n  );\n}\n```\n\n## 🔄 Migration Guide\n\n### Converting Existing Pages\n1. Replace custom containers with `PageContainer`\n2. Use `PageHeader` for consistent headers\n3. Replace custom stats with `StatsGrid`\n4. Wrap content in `ContentCard`\n5. Use `EmptyState` and `ErrorState` components\n6. Add loading states with skeleton components\n7. Apply consistent animations and hover effects\n\n### Status Badge Migration\n```tsx\n// Old\n<span className={getStatusColor(status)}>{status}</span>\n\n// New  \n<StatusBadge status={status} />\n```\n\n## 📚 Resources\n\n### Dependencies\n- **Tailwind CSS**: Utility-first CSS framework\n- **Radix UI**: Accessible component primitives\n- **Lucide React**: Icon library\n- **shadcn/ui**: Component library\n\n### File Structure\n```\nsrc/\n├── components/\n│   ├── layout/\n│   │   ├── pages/\n│   │   │   └── index.ts          # Design system components\n│   │   ├── DashboardLayout.tsx   # Main layout\n│   │   └── LayoutWrapper.tsx     # Layout wrapper\n│   └── ui/                       # Base UI components\n├── app/\n│   ├── globals.css               # Global styles & design tokens\n│   └── [role]/                   # Role-based pages\n└── lib/\n    └── utils.ts                  # Utility functions\n```\n\nThis design system provides a solid foundation for building consistent, accessible, and maintainable user interfaces across the entire application."